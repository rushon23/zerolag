"use client"

// The animated loader was removed. This component is kept as a no-op
// so the import in layout.tsx doesn't break during the cleanup.
export default function ClientAnimatedLoader() {
  return null
}
